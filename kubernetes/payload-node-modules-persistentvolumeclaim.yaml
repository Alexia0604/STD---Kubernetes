apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: payload-node-modules
  name: payload-node-modules
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
